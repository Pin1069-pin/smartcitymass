
import 'package:flutter/material.dart';

class FreeWiFiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Free Wi-Fi')),
      body: Center(child: Text('Show Free Wi-Fi locations on map.')),
    );
  }
}
