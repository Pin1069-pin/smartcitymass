
import 'package:flutter/material.dart';

class AboutCityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('About City')),
      body: Center(child: Text('Information about the city.')),
    );
  }
}
